<div class="jumbotron">
    <h1>Welcome</h1>
    <p>Gather is a place to meet and connect with others that want to set up events around conferences... </p>
    <span><a class="btn btn-primary btn-lg" {{action 'showModal' 'gathers.login-modal'}}>Login</a></span>
    {{#link-to 'gathers.signup' class="btn btn-primary btn-lg"}}Sign up{{/link-to}}
</div>


<div class="container">
  <div class="row">
    {{#each gather in model}}
      <div class="col-sm-6 col-md-4">
        <div class="thumbnail">
          <div class="index-thumbnail-container">
            <img class="index-thumbnail" {{bind-attr src=gather.gatherImage.url}} alt="" />
          </div>
          <div class="caption">
            <h3>{{gather.gatherName}}</h3>
            <p class="gather-description">{{gather.gatherDescription}}</p>
            {{link-to 'View Gather' 'gathers.gather' gather class="btn btn-primary" role="button"}}
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>
