<div class="container">
  <h2>We found {{gatherCount}} Gatherings.</h2>
  {{log model}}
</div>

<div class="container">
  <div class="row">
    {{#each gather in model}}
      <div class="col-sm-6 col-md-6 col-lg-6">
        <div class="thumbnail">
          <div class="index-thumbnail-container">
            <img class="index-thumbnail" {{bind-attr src=gather.gatherImage.url}} alt="" />
          </div>
          <div class="caption">
            <h3 class="gather-name">{{gather.gatherName}}</h3>
            <h4>{{moment gather.gatherDate}}</h4>
            <p class="gather-description">{{gather.gatherDescription}}</p>
            {{link-to 'View Gather' 'gathers.gather' gather class="btn btn-primary" role="button"}}
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>
